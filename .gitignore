node_modules/
build/
dist/
coverage/
.DS_Store

# Logs
*.log

# Environment — never commit real secrets
.env
.env.local
.env.production
!.env.example

# TLS certificates — never commit private keys
*.pem

# Database — contains user data
lvls.db
lvls.db-shm
lvls.db-wal

# Dev/temp files
test.txt
seed.js
SESSION_*.md
SPEC.md.txt
generate-certs.cjs
server.log

# BetterLeaks binary — install locally, do not commit the binary
betterleaks.exe
betterleaks
