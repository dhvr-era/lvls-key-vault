node_modules/
build/
dist/
coverage/
.DS_Store

# Logs
*.log

# Environment — never commit real secrets
.env
.env.local
.env.production
!.env.example

# TLS certificates — never commit private keys
*.pem

# Database — contains user data
lvls.db
lvls.db-shm
lvls.db-wal

# Python cache
__pycache__/

# Dev/temp files
test.txt
seed.js
SESSION_*.md
SPEC.md.txt
generate-certs.cjs
server.log

# BetterLeaks binary — install locally, do not commit the binary
betterleaks.exe
betterleaks

# Internal docs — audit reports, planning, and client scripts with infra details
# docs/ contains TLP:AMBER classified content — never push
docs/
AUDIT-*.md
*-security-audit.md
implementation_plan.md
lvls_client.py
lvls-client.ts
migrate-encrypt-db.mjs

# Unencrypted DB backups
*.unencrypted.bak
*.unencrypted.bak2
